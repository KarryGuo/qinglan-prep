# 青蓝备课 · Agent 评测报告

> 本报告由 `npm run eval` 自动生成，生成时间 2026-08-20 23:58，模型 `qwen-plus`，总耗时 11.6 分钟。

## 一、指标汇总

| 指标 | 数值 | 定义 |
|---|---|---|
| 阶段成功率 | 100.0% (12/12) | 四阶段运行中输出通过 zod Schema 校验并完成持久化的比例 |
| 课标引用真实率 | 100.0% (3/3) | 教学目标 curriculumRef 在课标库中真实存在的比例（评测独立查库复核，不信任编排层兜底） |
| 记忆写回正确率 | 100.0% (6/6) | memoryPatch 合法性 + 记忆迁移断言（保留/生长/消退）通过比例 |
| 工具调用覆盖率 | 100.0% (15/15) | 各阶段提示词要求的工具在事件流中实际被调用的比例 |
| 端到端完成率 | 100.0% (3/3) | 单用例四阶段（诊—设—生—思）连续完成的用例比例 |
| 多模态识别通过率 | 100.0% (4/4) | 成绩单照片识别（视觉模型）断言通过比例，与生产 OCR 路由同一份提示词与解析器 |
| 断言总通过率 | 100.0% (64/64) | 全部可机检断言通过比例 |
| 平均阶段耗时 | 58.1s | 单阶段端到端平均耗时 |

## 二、用例明细

### 用例 A《荷花》· 弱点延续

阶段执行：diagnose：✓（14.8s，工具：read_class_memory/search_textbook） · design：✓（72.9s，工具：search_curriculum/search_curriculum/search_textbook/search_curriculum/search_curriculum/search_curriculum/search_curriculum） · generate：✓（158.0s，工具：search_curriculum/search_questions/search_questions/search_questions/search_curriculum） · reflect：✓（33.0s，工具：read_class_memory/parse_scores）

溯源引用 18 条 · 执行事件留痕 47 条 · 断言 20/20 通过

| 断言 | 结果 | 说明 |
|---|---|---|
| diagnose 阶段完成（输出通过 zod Schema 校验并持久化） | ✓ | — |
| design 阶段完成（输出通过 zod Schema 校验并持久化） | ✓ | — |
| generate 阶段完成（输出通过 zod Schema 校验并持久化） | ✓ | — |
| reflect 阶段完成（输出通过 zod Schema 校验并持久化） | ✓ | — |
| diagnose 阶段调用了工具 read_class_memory | ✓ | — |
| diagnose 阶段调用了工具 search_textbook | ✓ | — |
| design 阶段调用了工具 search_curriculum | ✓ | — |
| generate 阶段调用了工具 search_questions | ✓ | — |
| reflect 阶段调用了工具 read_class_memory | ✓ | — |
| 诊断弱点与班级记忆锚定（至少一项与既有弱点对应） | ✓ | — |
| 教学目标的课标引用全部真实存在（独立复核） | ✓ | — |
| 各环节时长合计在 30-50 分钟（一节课合理范围） | ✓ | — |
| 教学环节含学情引用（classdata），体现对既有弱点的回应 | ✓ | — |
| 作业/随堂测知识点覆盖诊断出的弱点 | ✓ | — |
| 作业三档难度齐备（basic/advanced/extension） | ✓ | — |
| memoryPatch.resolved 仅含既有弱点名称（不许自造） | ✓ | — |
| 弱点「段落大意概括」仍显著（正确率未达标），记忆中必须保留 | ✓ | — |
| 四阶段全部完成（诊—设—生—思闭环） | ✓ | — |
| 溯源引用留痕 ≥ 5 条（Citation 表） | ✓ | — |
| Agent 执行事件留痕 ≥ 10 条（RunEvent 表，可追溯性） | ✓ | — |

### 用例 B《守株待兔》· 空记忆冷启动

阶段执行：diagnose：✓（34.3s，工具：read_class_memory/search_textbook） · design：✓（68.2s，工具：search_curriculum/search_curriculum/search_curriculum/search_textbook/search_curriculum/search_curriculum/search_curriculum） · generate：✓（72.6s，工具：search_curriculum/search_curriculum/search_questions/search_questions/search_questions/search_questions/search_questions/search_questions/search_curriculum） · reflect：✓（24.5s，工具：read_class_memory/parse_scores）

溯源引用 13 条 · 执行事件留痕 61 条 · 断言 19/19 通过

| 断言 | 结果 | 说明 |
|---|---|---|
| diagnose 阶段完成（输出通过 zod Schema 校验并持久化） | ✓ | — |
| design 阶段完成（输出通过 zod Schema 校验并持久化） | ✓ | — |
| generate 阶段完成（输出通过 zod Schema 校验并持久化） | ✓ | — |
| reflect 阶段完成（输出通过 zod Schema 校验并持久化） | ✓ | — |
| diagnose 阶段调用了工具 read_class_memory | ✓ | — |
| diagnose 阶段调用了工具 search_textbook | ✓ | — |
| design 阶段调用了工具 search_curriculum | ✓ | — |
| generate 阶段调用了工具 search_questions | ✓ | — |
| reflect 阶段调用了工具 read_class_memory | ✓ | — |
| 触发教师确认提问后，注入回答可完成阶段（人在环路可恢复） | ✓ | — |
| 教学目标的课标引用全部真实存在（独立复核） | ✓ | — |
| 各环节时长合计在 30-50 分钟（一节课合理范围） | ✓ | — |
| 作业/随堂测知识点覆盖诊断出的弱点 | ✓ | — |
| 作业三档难度齐备（basic/advanced/extension） | ✓ | — |
| memoryPatch.resolved 仅含既有弱点名称（不许自造） | ✓ | — |
| 空记忆班级经反思后长出新弱点（记忆从零生长） | ✓ | — |
| 四阶段全部完成（诊—设—生—思闭环） | ✓ | — |
| 溯源引用留痕 ≥ 3 条（Citation 表） | ✓ | — |
| Agent 执行事件留痕 ≥ 10 条（RunEvent 表，可追溯性） | ✓ | — |

### 用例 C《花钟》· 弱点消退

阶段执行：diagnose：✓（28.9s，工具：read_class_memory/search_textbook） · design：✓（75.2s，工具：search_curriculum/search_curriculum/search_curriculum/search_textbook/search_curriculum/search_curriculum/search_curriculum） · generate：✓（92.0s，工具：search_curriculum/search_curriculum/search_questions/search_questions/search_questions/search_questions/search_questions/search_questions/search_questions/search_questions） · reflect：✓（22.6s，工具：read_class_memory/parse_scores）

溯源引用 14 条 · 执行事件留痕 63 条 · 断言 21/21 通过

| 断言 | 结果 | 说明 |
|---|---|---|
| diagnose 阶段完成（输出通过 zod Schema 校验并持久化） | ✓ | — |
| design 阶段完成（输出通过 zod Schema 校验并持久化） | ✓ | — |
| generate 阶段完成（输出通过 zod Schema 校验并持久化） | ✓ | — |
| reflect 阶段完成（输出通过 zod Schema 校验并持久化） | ✓ | — |
| diagnose 阶段调用了工具 read_class_memory | ✓ | — |
| diagnose 阶段调用了工具 search_textbook | ✓ | — |
| design 阶段调用了工具 search_curriculum | ✓ | — |
| generate 阶段调用了工具 search_questions | ✓ | — |
| reflect 阶段调用了工具 read_class_memory | ✓ | — |
| 诊断弱点与班级记忆锚定（至少一项与既有弱点对应） | ✓ | — |
| 触发教师确认提问后，注入回答可完成阶段（人在环路可恢复） | ✓ | — |
| 教学目标的课标引用全部真实存在（独立复核） | ✓ | — |
| 各环节时长合计在 30-50 分钟（一节课合理范围） | ✓ | — |
| 教学环节含学情引用（classdata），体现对既有弱点的回应 | ✓ | — |
| 作业/随堂测知识点覆盖诊断出的弱点 | ✓ | — |
| 作业三档难度齐备（basic/advanced/extension） | ✓ | — |
| memoryPatch.resolved 仅含既有弱点名称（不许自造） | ✓ | — |
| 弱点「段落大意概括」正确率达标（≥90%），记忆应移除并记入已解决 | ✓ | — |
| 四阶段全部完成（诊—设—生—思闭环） | ✓ | — |
| 溯源引用留痕 ≥ 5 条（Citation 表） | ✓ | — |
| Agent 执行事件留痕 ≥ 10 条（RunEvent 表，可追溯性） | ✓ | — |

### 多模态：成绩单照片识别

视觉模型 `qwen-vl-plus` 识别 `eval/fixtures/score-table.png`（模拟成绩表截图：4 题，含 0.62 小数写法），耗时 0.8s。评测与生产 OCR 路由（`/api/lessons/[id]/reflect/ocr`）复用同一份提示词与解析器。

| 断言 | 结果 | 说明 |
|---|---|---|
| 照片识别输出可解析为结构化成绩（生产同款解析器） | ✓ | — |
| 识别出全部 4 道题的题号与正确率 | ✓ | — |
| 小数比例归一化为百分比（0.62 → 62） | ✓ | — |
| 全部正确率落在 0-100 且题号为正整数 | ✓ | — |

## 三、方法说明

1. **测的是产品本身**：评测直接复用生产编排 `runStage`（同一份提示词模板、工具注册表、zod Schema 校验与持久化逻辑），不经过任何评测专用旁路。
2. **断言全部可机检**：Schema 校验、课标引用存在性、工具调用（RunEvent 事件流）、记忆写回（ClassMemory 写回前后对比）均直接查库验证，无人工打分。
3. **独立评测库**：运行在 `prisma/eval.db`（本地 SQLite），与演示/生产数据完全隔离；知识库种子（课标 67 条 / 教材 21 节 / 题目 93 道）与线上一致。
4. **记忆闭环覆盖三种迁移**：用例 A 验证弱点仍显著时记忆必须保留（不许凭空清除）；用例 B 验证新接手班级从空记忆生长新弱点；用例 C 验证弱点达标后正确消退并记入已解决。
5. **多模态链路同样直测产品**：照片识别用例调用与生产 OCR 路由完全相同的提示词（`src/lib/ocr.ts`）与解析器，fixture 图（4 题成绩表，含 0.62 小数写法）随仓库提交，任何人可复现。
6. **波动性说明**：LLM 输出具有随机性（temperature 0.4），多次运行的指标可能在小范围内波动；失败断言如实记录、不修改产物，报告可由任何人运行 `npm run eval` 复现。

## 四、复现方式

```bash
npm run eval   # 初始化评测库 → 执行 3 用例 × 4 阶段 + 多模态照片识别 → 生成本报告与 JSON 明细
```

前提：`.env` 中配置可用的 `LLM_API_KEY`（多模态用例需同一服务支持视觉模型，默认 `qwen-vl-plus`，可用 `VLM_MODEL` 覆盖）；JSON 明细位于 `eval/reports/eval-<时间戳>.json`。
